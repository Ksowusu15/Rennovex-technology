"use client";
import { FormEvent,useState } from "react";
import { Loader2,Search,ShieldCheck } from "lucide-react";
type Result={type:string;reference:string;service?:string;status:string;preferredDate?:string;createdAt:string;updatedAt:string};
export function ClientPortalForm(){const[loading,setLoading]=useState(false);const[result,setResult]=useState<Result|null>(null);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");setResult(null);const payload=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/client-portal",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await r.json();setLoading(false);if(r.ok)setResult(data);else setError(data.error||"Request not found.")}return <div className="surface mx-auto w-full max-w-2xl p-4 sm:p-6 lg:p-7 xl:max-w-none"><form onSubmit={submit} className="space-y-4"><label className="label">Reference number<input className="field" name="reference" placeholder="Example: RNX-Q-..." required/></label><label className="label">Email used for the request<input className="field" name="email" type="email" placeholder="you@company.com" required/></label><button className="btn-primary w-full" disabled={loading}>{loading?<Loader2 className="animate-spin" size={17}/>:<Search size={17}/>} {loading?"Checking…":"Check request status"}</button></form>{error&&<div className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}{result&&<div className="mt-5 rounded-2xl border border-emerald-200 bg-emerald-50 p-5"><div className="flex items-center gap-2 font-bold text-emerald-800"><ShieldCheck size={19}/>Request verified</div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-emerald-700">Type</dt><dd className="font-semibold text-emerald-950">{result.type}</dd></div><div><dt className="text-emerald-700">Status</dt><dd className="font-semibold text-emerald-950">{result.status}</dd></div><div><dt className="text-emerald-700">Reference</dt><dd className="font-semibold text-emerald-950">{result.reference}</dd></div><div><dt className="text-emerald-700">Service</dt><dd className="font-semibold text-emerald-950">{result.service||"General"}</dd></div></dl></div>}</div>}
